/** Bump when stored JSON shape changes */
export const CONSENT_SCHEMA_VERSION = 1;

/** Bump when cookie banner / policy copy changes (re-prompt users) */
export const CONSENT_POLICY_VERSION = 1;

export const CONSENT_REGION = "PL";

export const STORAGE_KEY = "rs_cookie_consent_v1";

/** @typedef {'necessary'|'analytics'|'marketing'} ConsentCategory */
